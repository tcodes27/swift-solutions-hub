# -----------------------------------------------------------------------------
# Sprinter IT Hub — Base environment file (safe defaults, no secrets).
# Local overrides live in `.env.local`, which is git-ignored.
# -----------------------------------------------------------------------------

# Deployed Google Apps Script Web App endpoint.
# Leave empty here — set the real value in `.env.local`.
VITE_GOOGLE_APPS_SCRIPT_URL=
